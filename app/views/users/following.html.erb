<h3><%= @user.name %>さんのフォロー一覧</h3>

<% if @users.any? %>
  <%# Bootstrapのグリッドコンテナを開始 %>
  <div class="row">
    <% @users.each do |user| %>
      <%# 各ユーザーが1つのカラム（ボックス）になる %>
      <%# md（中画面）以上で6/12列を使用 = 1行に2つ %>
      <%# それ以下の画面幅では自動的に1行に1つになる %>
      <div class="col-md-6 mb-4">
        <div class="card h-100">
          <div class="card-body d-flex justify-content-between align-items-center">
            
            <%# ユーザー情報（名前のリンク） %>
            <div class="user-info">
              <%= link_to user.name, user_path(user), class: "fw-bold text-dark text-decoration-none" %>
            </div>
            
            <%# フォロー/アンフォローボタン %>
            <div class="user-actions">
                <%= render 'users/follow_unfollow_button', user: user %>
            </div>

          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <p>ユーザーをフォローしていません。</p>
<% end %>