

<!-- Hero Section -->
<section class="hero-gradient position-relative py-5">
    <div class="hero-overlay position-absolute top-0 start-0 w-100 h-100"></div>
    <div class="container position-relative text-center py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
               <div class="hero-text-overlay">

                <h2 class="display-4 font-serif fw-bold text-white mb-4">故人への想いを繋ぐ場所</h2>
                <p class="lead text-light mb-5">
                    有名人の墓所情報と訪問記録を共有する<br>
                    墓マイラーのためのコミュニティサイト
                </p>
                
                <!-- Search Bar -->
                <div class="row justify-content-center">
                  <div class="col-lg-10">
                    <%= search_form_for @q, url: search_path, method: :get, html: { class: "input-group input-group-lg" } do |f| %>
                      <%= f.search_field :name_cont,
                                         placeholder: "故人名を入力してください（例：坂本龍馬、織田信長）",
                                         class: "form-control search-input" %>
                      <%= f.submit "検索", class: "btn search-btn px-4" %>
                    <% end %>
                  </div>
                </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Search Options Section -->
<section class="py-5 bg-beige">
    <div class="container">
        <h3 class="text-center font-serif fw-bold text-primary-custom mb-5 display-6">墓所を探す</h3>
        <div class="row g-4">
            <!-- Genre Search -->
            <div class="col-lg-4">
                    <div class="search-option-card card h-100 border-0 shadow">
                        <div class="card-body text-center p-4">
                            <div class="search-icon-circle rounded-circle d-flex align-items-center justify-content-center mx-auto mb-4">
                                <i class="fas fa-book text-primary-custom fa-2x"></i>
                            </div>
                            <h4 class="card-title fw-bold text-primary-custom mb-3">ジャンルから探す</h4>
                            <p class="card-text text-muted mb-4">
                                明治維新・武将・文学・政治<br>
                                分野別に墓所を検索
                            </p>
                            <div class="d-flex flex-wrap gap-2 justify-content-center">

                            <% @genres.each do |genre| %>
                              <%= link_to genre.name, search_path(genre_name: genre.name), class: "category-badge badge" %>
                            <% end %>

                            </div>
                        </div>
                    </div>
            </div>
            
            <!-- Prefecture Search -->
            <div class="col-lg-4">
                    <div class="search-option-card card h-100 border-0 shadow">
                        <div class="card-body text-center p-4">
                            <div class="search-icon-circle rounded-circle d-flex align-items-center justify-content-center mx-auto mb-4">
                                <i class="fas fa-map-marked-alt text-primary-custom fa-2x"></i>
                            </div>
                            <h4 class="card-title fw-bold text-primary-custom mb-3">地域から探す</h4>
                            <p class="card-text text-muted mb-4">
                                全国47都道府県別<br>
                                お住まいの地域から検索
                            </p>
                            <div class="d-flex flex-wrap gap-2 justify-content-center">
                                <%= link_to "東京都", search_path(prefecture: "東京都"), class: "category-badge badge" %>
                                <%= link_to "京都府", search_path(prefecture: "京都府"), class: "category-badge badge" %>
                                <%= link_to "大阪府", search_path(prefecture: "大阪府"), class: "category-badge badge" %>
                                <%= link_to "福岡県", search_path(prefecture: "福岡県"), class: "category-badge badge" %>
                            </div>
                        </div>
                    </div>
            </div>
            <!-- Location Search -->
            <div class="col-lg-4">
                <div class="search-option-card card h-100 border-0 shadow">
                    <div class="card-body text-center p-4" data-controller="location-search">
                        <div class="search-icon-circle rounded-circle d-flex align-items-center justify-content-center mx-auto mb-4">
                            <i class="fas fa-location-arrow text-primary-custom fa-2x"></i>
                        </div>

                        <h4 class="card-title fw-bold text-primary-custom mb-3">現在地から探す</h4>
                        <p class="card-text text-muted mb-4">
                            GPS機能を使って<br>
                            近くの墓所を検索
                        </p>
                        
                        <button class="btn btn-gold location-btn" data-action="click->location-search#search">
                            <i class="fas fa-crosshairs me-2"></i>位置情報を取得
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Latest Posts Section (投稿がある場合のみ表示) -->
<div class="container-fluid my-5 px-md-5"> <%# container-fluidに変更し、左右のパディングを追加 %>
  <h2 class="text-center mb-4">最新の記録</h2>
  <div class="row">
    <% @posts.each do |post| %>
      <div class="col-12 col-md-4 mb-4">
        <%= render 'posts/post_card', post: post %>
      </div>
    <% end %>
  </div>
</div>


<!-- Call to Action -->
<section class="cta-section py-5 text-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h3 class="font-serif fw-bold text-white mb-4 display-6">墓マイラーコミュニティに参加しませんか？</h3>
                <p class="lead text-light mb-5">あなたの墓参り体験を共有し、同じ想いを持つ仲間と繋がりましょう</p>
                <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center">
                    <%= link_to new_user_registration_path, class: "btn btn-gold btn-lg cta-btn-primary" do %>
                        <i class="fas fa-user-plus me-2"></i>無料で始める
                    <% end %>
                   <%= link_to about_path, class: "btn btn-outline-light btn-lg cta-btn-secondary" do %>
                        <i class="fas fa-info-circle me-2"></i>詳しく見る
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</section>

